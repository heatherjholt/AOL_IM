# AOL_IM
AOL Style Instant Messenger that can communicate across two devices connected to the same local network.

*Currently configured for local 127.0.0.1, but can work with two devices on local network by updating IP.*

Open two terminals, run python server.py in one and python client.py in the other. 